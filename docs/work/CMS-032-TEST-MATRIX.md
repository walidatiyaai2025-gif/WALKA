# CMS-032 regression matrix

The automated feature suite covers admin authentication, valid assignment/reorder, optimistic revision conflicts, wrong-purpose/Draft/missing-canonical rejection, duplicate assets, duplicate/gapped positions, max gallery size, deterministic product fallback, explicit variant override, archive invalidation, public metadata allowlisting and stale admin mutation preservation.
