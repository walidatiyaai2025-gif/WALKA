# CMS-032 validation gate

Merge only after the normal Backend API and Flutter release/regression workflows are Green on the exact PR head. After merge, reconcile roadmap/work evidence on stable `main`. Visual-release issue #230 remains independent and open unless its own definition of done is met.
