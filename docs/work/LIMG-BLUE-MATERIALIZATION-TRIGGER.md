# LIMG Blue materialization trigger

This branch exists only to execute the repository's pre-existing deterministic Blue canonical admission workflow against the fingerprint-locked approved source. The resulting canonical blob is harvested into the active PAV branch; this staging branch is not a release branch.
