# PDP-003 — Extract PDP gallery viewport

Status: IN PROGRESS
Issue: #171
Branch: `agent/pdp-003-gallery-viewport`
Parent: #85 UI Task Master

## Scope

Extract only the active V111 Product Detail gallery viewport while keeping page-controller/selected-index ownership in the existing gallery state so PDP-004 can independently extract the indicator controls.

## Delivered on task branch

- Added `mobile/lib/features/products/presentation/widgets/walka_pdp_gallery_viewport.dart`.
- Preserves the released `1.02` aspect ratio and 18px clipped material radius.
- Preserves the three product views and their exact padding/rotation/scale treatment.
- Preserves per-view product semantics.
- Preserves tap-to-expand behavior and the top-right fullscreen affordance.
- Preserves the selected-page `n / 3` badge.
- Keeps `PageController` and selected-index state caller-owned.
- Adds focused tests for geometry/page count, semantics/page-change callback, page tap expansion, fullscreen selected-index behavior and count badge.

## Guardrails

- PDP-004 indicator row is not extracted or redesigned in this slice.
- PDP-005 fullscreen route/viewer is not extracted or redesigned in this slice.
- No gallery content, product facts, variants, favorites, share, Amazon CTA or Product Master changes.
- `Images/` untouched.
- No ZIP delivery.

## Required validation before stable merge

- [ ] Integrate viewport into active V111 gallery without visual/behavior change.
- [ ] `flutter analyze` Green.
- [ ] Full Flutter tests Green.
- [ ] Android runner / WALKA branding Green.
- [ ] Release APK build Green.
- [ ] PR-context validation Green.
- [ ] Merge to stable `main` only after required gates pass.
