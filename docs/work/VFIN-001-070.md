# VFIN-001..070 — Final production-media release gates

Status: IN PROGRESS
Parent batch: #275
Visual blocker: #230

This branch consolidates the executable, non-fabricating gates for VFIN-001..070.

## Gray admission
Drawer Gray is fail-closed. A bundled `gray.png` is not evidence of production admission. Promotion is valid only when the current source-admission and provenance authorities report an APPROVED source and the canonical binary passes the repository PAV rules. No generic White recolor, hidden-geometry reconstruction, or protected `Images/` mutation is permitted.

## Five-variant integrity
The release registry remains exactly Drawer White/Gray and Lunch Blue/Pink/Green at the existing canonical paths. Both product directories must remain bundled, all five registered files must be present and decodable, and any ADMITTED binary must carry deterministic metadata and complete visual-QA PASS states.

## Quality/performance
Every ADMITTED binary is rechecked with the existing `PavPngInspector`: 1024×1024, 8-bit RGBA, embedded color profile, transparent perimeter, safe margins, and provenance byte/dimension parity. This reuses PAV/PIMG rather than creating a competing validator.

## Release truth
Source admission and production provenance must remain mutually valid; admitted + pending + blocked must equal five. The visual release can only be mechanically 5/5 when no pending/blocked variant remains.

Mechanical media readiness is deliberately separated from owner visual acceptance. Automated code never claims owner visual PASS.

Completion evidence is appended only after protected reference guard, Analyze, full Flutter tests, release APK, merge, and latest-main validation are Green.
