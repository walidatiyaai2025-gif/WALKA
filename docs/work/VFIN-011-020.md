# VFIN-011..020 — Five-variant production-media integrity

Status: IN PROGRESS
Parent batch: #275

Executable coverage on this branch locks the released production-media registry to exactly five variants and canonical paths, requires all five repository files to exist under `assets/products/**`, proves both product directories are bundled by `pubspec.yaml`, validates PNG signatures and the 1.2 MiB hard budget, requires complete visual-QA state keys, rejects protected `Images/` runtime paths, and proves every canonical asset is bundle-decodable.

For any row that reaches `ADMITTED`, the suite additionally requires deterministic SHA-256/size/dimensions, all mandatory visual-QA checks at PASS, and exact byte-size parity with the bundled file. Blocked media is explicitly forbidden from masquerading as admitted.

This wave does **not** turn Gray into production media by assertion. Gray remains governed by VFIN-001..010 and the source-admission truth.

Completion evidence will be appended after branch CI and latest-main validation are Green.
